var result 		= {},	_			= require('underscore'),	randomStr 	= require('random-string'); //Not using this. Maybe later. Oct 16 2013.result.generatePassword = function(){	var text = "";    var possible = "ABCDEFGHIJKLMNOPQRSTUVWXYZabcdefghijklmnopqrstuvwxyz0123456789";    for( var i=0, len = _.random(8, 12); i < len; i++ )        text += possible.charAt(Math.floor(Math.random() * possible.length));    return text;};result.getRandomPerson = function() {	var personCollection = ds.Person.all(),		len = personCollection.length,		winnerIndex = _.random(0, len-1);			return personCollection[winnerIndex];};module.exports = result;